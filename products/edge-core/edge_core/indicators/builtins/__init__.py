"""Builtin indicators package (skeleton-compatible).

Add indicator implementations here (e.g., momentum, volatility). These can be simple
functions registered via a decorator or classes deriving from the local Indicator base,
depending on which registry path you wire up.
"""

