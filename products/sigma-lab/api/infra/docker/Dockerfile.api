# Skeleton Dockerfile for API
FROM python:3.10-slim
WORKDIR /app
COPY . /app
ENV PYTHONPATH=/app/products/sigma-lab:/app/products/sigma-core:/app/products/sigma-platform
RUN pip install --no-cache-dir -r requirements.txt || true
CMD ["uvicorn", "api.app:app", "--host", "0.0.0.0", "--port", "8001"]
