# Sigma Sim — placeholder Makefile

.PHONY: help api ui

help:
	@echo "Sigma Sim (simulation & scenarios). When split to api/ui repos, add:"
	@echo "  api    → uvicorn api.app:app"
	@echo "  ui     → vite dev"

api:
	@echo "[placeholder] run API"

ui:
	@echo "[placeholder] run UI"

