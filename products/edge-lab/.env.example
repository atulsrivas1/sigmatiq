# API keys
POLYGON_API_KEY=your_polygon_api_key
# Optional legacy env name supported by adapters
ZE_POLYGON_API_KEY=

# Caching
EDGE_CACHE_DIR=./products/edge-lab/data_cache
PACKS_DIR=./products/edge-lab/packs

# Database (Edge Lab)
DB_HOST=localhost
DB_PORT=5432
DB_USER=edge_user
DB_PASSWORD=secret
DB_NAME=edge_lab
# Optional: per-product schema (avoids using public)
DB_SCHEMA=app
POLYGON_VERBOSE=0

# Database (set per product; see docs/DB_NAMING_AND_SEPARATION.md)
# Examples: edge_lab (Edge Lab), edge_sim (Edge Sim), edge_market, edge_pilot
DB_HOST=localhost
DB_PORT=5432
DB_USER=edge_user
DB_PASSWORD=secret
DB_NAME=edge_lab
# Optional schema (default: public or app)
# DB_SCHEMA=app

# FastAPI / Uvicorn
# UVICORN_HOST=0.0.0.0
# UVICORN_PORT=8001
