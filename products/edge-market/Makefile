# Edge Market — placeholder Makefile

.PHONY: help api ui

help:
	@echo "Edge Market (marketplace). When split to api/ui repos, add:"
	@echo "  api    → uvicorn api.app:app"
	@echo "  ui     → vite dev"

api:
	@echo "[placeholder] run API"

ui:
	@echo "[placeholder] run UI"

