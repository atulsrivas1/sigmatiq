# API keys
POLYGON_API_KEY=your_polygon_api_key
# Optional legacy env name supported by adapters
ZE_POLYGON_API_KEY=

# Caching
EDGE_CACHE_DIR=./data_cache
POLYGON_VERBOSE=0

# Database (optional for registry/versioning)
DB_HOST=localhost
DB_PORT=5432
DB_USER=sigmatix
DB_PASSWORD=secret
DB_NAME=sigmatix_edge

# FastAPI / Uvicorn
# UVICORN_HOST=0.0.0.0
# UVICORN_PORT=8001

# Packs and product workspace
# Where strategy packs (configs, policies, indicator sets) live
# You can also set SIGMA_PACKS_DIR (takes precedence) if needed
PACKS_DIR=./products/sigma-lab/packs

# Where to write product outputs (matrices, artifacts, reports, live_data)
# Set SIGMA_PRODUCT_DIR to override the default (products/sigma-core/sigma-lab)
# Example to keep everything under products/sigma-lab:
# SIGMA_PRODUCT_DIR=./products/sigma-lab

# Indicator set cache (temp file shim used by engine)
# Override the cache directory (default: <SIGMA_PRODUCT_DIR>/reports/indicator_cache)
# INDICATOR_CACHE_DIR=./.cache/indicator_sets

